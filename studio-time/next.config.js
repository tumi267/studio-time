/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['plus.unsplash.com', 'unsplash.com','images.unsplash.com'], // Add unsplash domains here
    },
  }
module.exports = nextConfig
